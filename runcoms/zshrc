#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

export PURE_PROMPT_SYMBOL='%(!.#.$)'
export LC_ALL=en_US.UTF-8

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Aliases
alias please='sudo $(fc -nl -1)'
alias vim=`if [ -x "$(command -v nvim)" ]; then echo nvim; else echo vim; fi`
alias showip='echo $(curl -s ifconfig.me)'
alias nvm-init='source /home/leo/code/software/nvm/nvm.sh'
alias stripexif='-makernotes:all= -composite:all= '
alias gc='git commit --no-verify'
alias gcd='git branch | fzf | xargs git checkout'
grb() {
  git fetch -p && for branch in $(git for-each-ref --format '%(refname) %(upstream:track)' refs/heads | awk '$2 == "[gone]" {sub("refs/heads/", "", $1); print $1}'); do git branch -D $branch; done
}

alias ll='exa -al --git --git-ignore'
alias la='exa -al --git'
alias lat='exa -al --git --git-ignore -T'
alias lat1='exa -al --git --git-ignore -T --level=1'
alias lat2='exa -al --git --git-ignore -T --level=2'
alias lat3='exa -al --git --git-ignore -T --level=3'

# Environment
export EDITOR=nvim
export GIT_EDITOR=nvim
export XDG_CONFIG_HOME=$HOME/.config

## Go
export GOPATH=$HOME/go
export GO111MODULE=on

# fzf
export FZF_DEFAULT_COMMAND="rg -l --files"
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh


export PATH="$HOME/.local/bin:$HOME/.yarn/bin:$PATH:$HOME/code/software/dart-sdk/bin:$HOME/.pub-cache/bin:$HOME/code/software/flutter/bin:$HOME/code/software/aws/bin"

export AUTO_NOTIFY_THRESHOLD=30
source /home/leo/code/software/zsh-auto-notify/auto-notify.plugin.zsh

# allow podman to access the docker registry file
export REGISTRY_AUTH_FILE=/home/leo/.docker/config.json


